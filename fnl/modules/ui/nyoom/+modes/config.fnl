(local {: autoload} (require :core.lib.autoload))
(local {: setup} (autoload :modes))

(setup {:colors {:insert "#5576cf"
                 :delete "#df2566"
                 :visual "#569aac"
                 :copy   "#45ac57"}})
